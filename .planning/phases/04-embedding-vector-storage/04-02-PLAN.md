---
phase: 04-embedding-vector-storage
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified: ["src/core/db.py"]
autonomous: true
requirements: ["EMB-03"]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Chroma DB persists to disk and can be loaded in a fresh Python process without re-embedding"
  artifacts:
    - path: "src/core/db.py"
      provides: "ChromaDB client initialization"
      contains: ".data"
  key_links: []
---

<objective>
Fix ChromaDB persistence directory to align with project gitignore rules.

Purpose: Prevent accidental commits of large vector database files.
Output: Updated `src/core/db.py` using `.data/` directory.
</objective>

<context>
@.planning/phases/04-embedding-vector-storage/04-01-VERIFICATION.md
@src/core/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ChromaDB persist directory</name>
  <files>src/core/db.py</files>
  <action>
    Update `get_chroma_client` in `src/core/db.py` to use `.data` instead of `data` for the `persist_directory`.
    
    Gap reason: ChromaDB persists to `data/chroma_db` instead of the project standard `.data/chroma_db`. Since `data/` is not in `.gitignore`, this risks committing large database files to the repository.
    
    Change:
    ```python
    persist_directory = os.path.join(
        os.path.dirname(os.path.dirname(os.path.dirname(__file__))), ".data", "chroma_db"
    )
    ```
  </action>
  <verify>
    <automated>python -c "import src.core.db as db; assert '.data' in db.get_chroma_client().get_settings().persist_directory"</automated>
  </verify>
  <done>Chroma DB persists to the ignored `.data/` directory</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System -> Git | Prevent PII/vector data from leaking into source control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | src/core/db.py | mitigate | Ensure database is created in `.data/` which is ignored by git |
</threat_model>

<verification>
python -c "import src.core.db as db; assert '.data' in db.get_chroma_client().get_settings().persist_directory"
</verification>

<success_criteria>
- `src/core/db.py` is updated
- The database is initialized in the `.data` folder
- `data/chroma_db` is no longer used
</success_criteria>

<output>
After completion, create `.planning/phases/04-embedding-vector-storage/04-02-SUMMARY.md`
</output>