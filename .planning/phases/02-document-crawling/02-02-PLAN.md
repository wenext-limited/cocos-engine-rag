---
phase: 02-document-crawling
plan: 02
type: execute
wave: 2
depends_on:
  - 01-infrastructure-project-scaffold
  - 02-01
files_modified:
  - src/crawler.py
autonomous: true
requirements:
  - DOC-03
gap_closure: true
must_haves:
  truths:
    - "Crawler respects a configurable delay between requests and retries on failures"
  artifacts:
    - path: "src/crawler.py"
      provides: "Retry mechanism on failed `session.get` calls"
      contains: "Retry"
  key_links:
    - from: "src/crawler.py"
      to: "requests.Session"
      via: "HTTPAdapter with Retry"
      pattern: "HTTPAdapter"
---

<objective>
Implement missing network retry logic in the document crawler to ensure failed downloads are retried automatically, satisfying the gap identified during verification.

Purpose: Make the document crawler resilient to intermittent network failures, ensuring pages are not permanently skipped.
Output: Updated `src/crawler.py` with `urllib3.util.Retry` integrated into the `requests.Session`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-document-crawling/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Retry Logic to Crawler Session</name>
  <files>src/crawler.py</files>
  <action>
    Update the HTTP session setup in `src/crawler.py` to use `requests.adapters.Retry` and `requests.adapters.HTTPAdapter`.
    
    1. Import `Retry` from `urllib3.util.retry` and `HTTPAdapter` from `requests.adapters`.
    2. Configure the existing `requests.Session()` with an `HTTPAdapter` that uses a robust `Retry` policy (e.g., `total=5`, `backoff_factor=1`, `status_forcelist=[429, 500, 502, 503, 504]`).
    3. Mount the adapter to both `http://` and `https://` prefixes on the session.
    4. Review any existing `try-except` blocks around `session.get` to ensure they don't prematurely swallow exceptions before the retry logic completes. Permanent failures (after retries are exhausted) should still be handled gracefully without crashing the whole process, but retries MUST occur first.

    Gap reason: The script implemented rate limiting (delay) but lacked retry logic for network failures. Failed requests were skipped and the URL was marked as visited permanently.
  </action>
  <verify>
    <automated>python -c "import ast; code=open('src/crawler.py').read(); assert 'Retry' in code and 'HTTPAdapter' in code, 'Missing Retry/HTTPAdapter imports or usage'"</automated>
  </verify>
  <done>Crawler session uses an HTTPAdapter with Retry policy for resilience</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network | External HTTP requests to docs.cocos.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | src/crawler.py | mitigate | Using backoff_factor in Retry ensures we do not slam the server on errors, extending the existing delay logic. |
</threat_model>

<verification>
Check that `src/crawler.py` mounts an `HTTPAdapter` with `Retry` configuration to the HTTP session.
</verification>

<success_criteria>
The crawler correctly retries on 500/502/503/504 and transient network errors before giving up.
</success_criteria>

<output>
After completion, create `.planning/phases/02-document-crawling/02-02-SUMMARY.md`
</output>